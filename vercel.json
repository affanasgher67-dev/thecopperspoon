{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "env": {
    "PYTHONPATH": "src"
  },
  "functions": {
    "app.py": {
      "maxDuration": 60,
      "includeFiles": "{src/**,pyproject.toml,uv.lock,requirements.txt,data/**}"
    }
  }
}
