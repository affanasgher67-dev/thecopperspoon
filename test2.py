import urllib.request, json; key=open('api_key.txt').read().strip(); data=json.dumps({'contents':[{'role':'user','parts':[{'text':'hi'}]}]}).encode(); req=urllib.request.Request(f'https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash:generateContent?key={key}', data=data, headers={'Content-Type':'application/json'}); print(urllib.request.urlopen(req).read()[:50])
